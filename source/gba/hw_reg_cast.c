// Deliberately does not include the main header
// so that they can disagree about the types involved

#include <stdint.h>

uint16_t bgcnt2uint(uint16_t x) {
	return x;
}
uint16_t keypad2uint(uint16_t x) {
	return x;
}
